# THIS SCRIPT IS TO INSTALL AEOLUS BY:
# Install all the rpms from the last released repo and then pull all the src and compile
# Install from the src
# Update aeolus components

# Usage:
$ python src/install.py --help

# Install from repo
$ python src/install.py --source-yum

# Install from src:
$ python src/install.py --source=git --base_dir '/home'

# Update a specific component
$ python src/install.py --source=git --base_dir '/home/xyz' imagefactory
